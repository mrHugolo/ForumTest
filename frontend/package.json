{
  "name": "frontend",
  "version": "0.0.0",
  "scripts": {
    "idep": "cd ../backend && npm i && cd ../frontend && npm i",
    "both": "concurrently --kill-others \"nodemon ../backend/server.js\" \"npm run dev\"",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-icons": "^4.3.1",
    "react-router-dom": "5.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^1.0.7",
    "concurrently": "^7.0.0",
    "vite": "^2.7.2"
  }
}
